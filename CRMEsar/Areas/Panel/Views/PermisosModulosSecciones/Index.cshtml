@model IEnumerable<CRMEsar.Models.ViewModels.CrudEntidades.PermisosModulosSecciones.PermisosModulosSeccionesTablaVM>
@{
    ViewData["Title"] = "Lista De Permisos";
}
<div class="container mt-4">
    <div class="row">
        <div class="col-8">
            <h2 class="text-primary"> Lista de permisos para los modulos o secciones</h2>
        </div>
        <div class="col-4 text-right">
            <a asp-action="Create" class="btn btn-primary" style="background:#1B75BB ; color:white;border:none"><i class="bi bi-plus-circle-fill"></i>&nbsp; Agregar permiso</a>
        </div>
    </div>
    <br />
    <br />
    @if (TempData["RespuestaOperacion"] != null)
    {
        <div class="alert alert-success text-center">
            @TempData["RespuestaOperacion"]
        </div>
    }
    <div class="table-responsive">
        <table id="TblContenido" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th> Rol </th>
                    <th> Modulo </th>
                    <th> Seccion </th>
                    <th> ¿Es temporal? </th>
                    <th> Acciones </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var permisoModuloSecciones in Model)
                {
                    <tr>
                        <td>@permisoModuloSecciones.Rol</td>
                        <td>@permisoModuloSecciones.Modulo</td>
                        <td>@permisoModuloSecciones.Seccion</td>
                        <td>@(permisoModuloSecciones.Temporal ? "Si":"No")</td>
                        <td>
                            <a asp-route-id="@permisoModuloSecciones.PermisomoduloSeccionIdEncriptado" asp-action="Edit" class="btn btn-success text-white" style="background:#C63D96 ; color:white;border:none;cursor:pointer; width:140px;">
                                <i class="far fa-edit"></i> Editar
                            </a>
                            &nbsp;
                            <a onclick=Delete() class="btn btn-danger text-white" style="background:#EE3A4F ; color:white;border:none;cursor:pointer; width:140px;">
                                <i class="far fa-trash-alt"></i> Borrar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section Scripts {
    <script src="~/js/Maestros/dataTableGenerico.js"></script>
}